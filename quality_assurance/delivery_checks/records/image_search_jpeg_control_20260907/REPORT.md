# 同图 JPEG 对照查询

核验时间：2026-09-07 01:15:50（Asia/Shanghai）。用户明确允许追加一次同图JPEG查询，最多返回3款。

**结果：本地JPEG上传返回3款，原WebP网址查询返回0款。** 本次保持品池4306497、高相关性high、采购件数1、上限3不变，没有主体裁剪。当前已验证同图下载、转JPEG、上传图片内容这条路径能够召回候选；原网址取图/WebP输入路径仍是首要排查方向。尚不能区分外链抓取、WebP解码或服务时点变化的影响，不能宣称已经证明某一种后端错误。

## 对照证据

| 项目 | 原查询 | 本次查询 |
|---|---|---|
| 输入 | TikTok CDN WebP网址 | 同一主图转换后的本地JPEG，800×800 |
| 图片内容上传 | 无，只传imageUrl | Base64，86,594字节；网关回填字节与上传SHA-256一致 |
| 品池 / 相关性 / 采购件数 / 上限 | 4306497 / high / 1 / 3 | 4306497 / high / 1 / 3 |
| 返回数量 | CLI商品列表0 | 网关原始列表3、CLI列表3 |
| 原始HTTP记录 | 未保存 | HTTP 200，success=true，msgCode=OK |

原查询见[历史CLI快照](../../../../workflow_execution/run_history/records/20260906T165943Z_fc9a7de8/searches/QI-eb1c8aff42a0fae9.json)；输入来源与转换检查见[前一阶段诊断](../image_search_diagnosis_20260907/REPORT.md)。

本次保存[调用清单](manifest.json)、[实际请求摘要](search_attempt.json)、[HTTP状态](http_metadata.json)、[网关原始响应](gateway_response.json)、[完整CLI输出](cli_stdout.json)和[完整工具商品表](provider_output.md)。网关响应包含其回填的图片Base64，不需要在界面展示；未保存认证请求头或凭据。

## 结论范围

- 该品池与high条件下可以返回候选，无须为本次结果改品池或降低相关性。
- 这一次真实结果支持优先改进图片输入路径，但只覆盖这张主图；不是一般化的接口可靠性验证。
- 项目生产适配器仍使用URL路径，本次没有修改默认流程。后续实现应保留原图来源、下载与转换哈希，在首次查询前准备JPEG，并继续遵守查询上限；不要把空结果自动重试当成已授权的无限找货。
- 原商品被评为偏大，但原购SKU的实际尺寸仍缺失。三款候选分别返回S/驼色、S/卡其色、M/黑色，只有尺码标签不能判定解决偏大。面料比例、具体尺码表与实际供货均未核验；第2款起批量返回50，第3款起批量返回2，不能把采购输入1解释为都支持1件起订。[SKU待核对记录](candidate_sku_review.json)保留具体商品及SKU身份，未知尺寸与成分保持空值。

## 验收与运行说明

[核验记录](verification.json)的10项检查通过，包括一次实际搜索请求、前后参数一致、图片哈希、网关与CLI的商品/SKU身份一致及原始响应哈希。没有新增第二次请求，没有供应商消息或订单。

诊断脚本在保存全部结果、CLI正常退出之后，最后向GBK终端打印勾号字符时发生UnicodeEncodeError，导致外层进程退出码1。这是结果显示错误；已从UTF-8存档核对实际HTTP和CLI结果。没有因该错误重跑查询。实际执行脚本保留为[run_once.py](run_once.py)，已有尝试标记会阻止它再次发起搜索。
